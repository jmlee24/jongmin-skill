# 쓰기 레인 프롬프트 템플릿

`general-purpose` 에이전트 + T2([model-tiers.md](model-tiers.md))로 발진한다. 아래 5요소를 **모두** 채운다 —
빈 요소가 있으면 발진하지 않는다.

```
[임무] <구현 대상 한 줄 + 왜 하는지 한 줄>

1) 필독 문서
   <루트/폴더 CLAUDE.md, 테스트 규약, 관련 설계 문서의 경로 목록.
    "읽어라"가 아니라 경로를 나열한다.>

2) 커밋 단위
   <분할 계획: 커밋 1 = ..., 커밋 2 = ...>
   메시지 형식: <프로젝트 형식 그대로>. push 금지.

3) 검증 요구
   <프로젝트가 요구하는 가장 강한 오라클을 명시.>
   - 등가성 작업이면 비트/바이트 동일성 수준(직렬화 결과 비교 등)을 요구한다.
     근사 비교(allclose류)를 등가성 주장에 쓰는 것 금지.
   - 기준선/카운터가 있으면 재잠금 방향을 명시한다.

4) 규약 복제
   <프로젝트 메모리·수치 표기 규약 등, 에이전트가 볼 수 없는 규칙의 전문을 여기 붙인다.
    에이전트는 이 프롬프트 밖의 컨텍스트를 못 본다.>

5) 보고 형식
   - 커밋 해시 목록
   - 테스트 결과 원문 (요약 금지)
   - 사양 이탈 판단은 근거와 함께 전부 보고.
     사양이 틀렸다고 판단되면 구현을 멈추고 실측 근거로 보고하라 —
     그것이 최선의 산출물이다.
```

## 채워진 실물 예시

CSV 파서의 무손실 최적화 웨이브에서 실제로 쓸 수 있는 수준의 예시:

```
[임무] parse_rows()의 행 단위 dict 생성을 컬럼 벡터 방식으로 교체한다.
프로파일 실측에서 전체 시간의 61%가 이 함수에 있고, 출력 등가성이 보존 조건이다.

1) 필독 문서
   - CLAUDE.md (루트)
   - tests/README.md (테스트 실행 규약)
   - docs/data-format.md (출력 스키마 정의)

2) 커밋 단위
   커밋 1 = 등가성 하니스 추가 (기존 출력을 pickle로 고정하는 테스트)
   커밋 2 = parse_rows() 벡터화 (하니스 green 유지)
   메시지 형식: `refactor: 한국어 설명`. push 금지.

3) 검증 요구
   - 커밋 1의 하니스: 샘플 3종에 대해 기존 구현 출력을 직렬화(pickle.dumps) 후 바이트 비교로 고정
   - 커밋 2 이후 같은 하니스 green — allclose·근사 비교 금지, 바이트 동일성만 인정
   - pytest tests/ 전체 green, 신규 실패 0

4) 규약 복제
   - 수치 표기: 소수점 이하 6자리 고정, 지수 표기 금지 (docs/data-format.md 3절 전문: "...")
   - 함수는 단일 책임, 매직 넘버는 상수로 추출
   - 새 문서 파일 생성 금지

5) 보고 형식
   - 커밋 해시 목록
   - pytest 출력 원문 (요약 금지)
   - 하니스가 잡아낸 차이가 있으면 사양 이탈로 간주하고 구현을 멈추고 바이트 diff와 함께 보고
```

## 실험 모드 확장 — contract-isolated write lanes 한정

쓰기 병렬 실험(conductor 실험 모드)에서만 5요소에 더해 6번째 요소를 필수로 채운다:

```
6) 소유권·계약
   - Owned files: <이 레인만 수정 가능한 파일 목록>
   - Forbidden files: <읽기만 허용, 수정 금지 — 공용 타입·픽스처·config·lockfile 포함>
   - Frozen contracts: <동결 커밋 SHA + 동결된 시그니처·스키마 목록>
   - Integration assumptions: <다른 레인 산출물에 대한 가정>
```

소유권 밖 파일 수정이나 동결 계약 변경이 필요해지면 **구현을 멈추고 보고**한다 —
그것이 최선의 산출물이다.

## 주의

- 새 문서 파일 생성 금지 (임무가 명시적으로 요구할 때만) — 문서화는 기존 문서 갱신으로,
  계획·요약은 보고에 포함하고 파일로 남기지 않는다.
- 같은 파일을 만지는 쓰기 레인을 병렬로 띄우지 않는다.
- 선행 커밋이 있는 웨이브 중반 발진이면, CX 레인 명세가 본 트리와 현재 트리의
  차이(합류 지점)를 프롬프트에 명시한다.

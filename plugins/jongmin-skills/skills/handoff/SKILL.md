---
name: handoff
description: 세션 경계에서 작업 상태를 인수인계할 때 사용한다 — 컨텍스트 압축(컴팩션) 임박, 계정 전환(개인/법인), 기기 이동, 웨이브 중단 시 "handoff", "인수인계", "스냅샷 떠줘", "이어서 할 수 있게"를 요청하면 발동한다. 단순 작업 요약 요청에는 사용하지 않는다.
argument-hint: "<save|restore> [메모]"
---

# handoff — 세션·계정 인수인계

정체성: **세션 경계에서의 무손실 상태 이전**. 얇은 절차형 스킬 — T1 직접 수행, 위임 없음.
conductor·loop·sortie의 "컴팩션·중단 후 재개" 갭을 이 스킬이 담당한다.

## save — 스냅샷 생성

두 모드가 있다:
- **`save` (기본, 이탈 저장)** — 스냅샷 후 활성 백그라운드 잡 정지, 생성한 worktree 제거.
- **`save --keep-lanes` (중간 저장)** — 레인을 살려둔 채 체크포인트만 뜬다. 스냅샷에 레인 ID·시작
  시각·데드라인·산출물 위치와 함께 **"레인을 정지하지 않았음" 경고를 반드시 기록**한다.
  (실전 검증에서 기본 save가 진행 중인 CX 감사를 죽일 뻔한 사례에서 분리 — Codex 논의 합의안.)

위치: `~/.claude/jongmin-ledgers/<프로젝트명>/<YYYY-MM-DD>-handoff.md` ([ledger.md](../../shared/ledger.md) 경로 규칙).

> 턴 주의: deep-audit 등 `disallowed-tools`를 쓰는 스킬과 같은 턴이면 Write가 차단될 수 있다 (실측).
> 이 경우 handoff는 별도 턴에서 실행하라.

```
- [ ] 기준 상태: 브랜치, HEAD SHA, 워킹트리 상태(더티 파일 목록), 미커밋 diff 요약
- [ ] 진행 장부 위치와 최종 상태 (진행/유예/실패 항목)
- [ ] 활성 레인: 백그라운드 잡·에이전트·codex 스레드 ID와 각각의 상태
- [ ] 다음 단계: 재개 시 제일 먼저 할 일 1~3개 (명령 수준으로 구체적으로)
- [ ] 유예된 결정: 사용자 판단 대기 항목
- [ ] 계정 회계: 이 작업이 소모한 계정(개인|법인), 재개 권장 계정
- [ ] 민감정보 미포함 확인 (토큰·키 금지)
```

스냅샷 후 (기본 save만): 활성 백그라운드 잡 정지, 생성한 worktree 제거, 중단 상태를 사용자에게 보고.
`--keep-lanes`면 레인 상태 요약만 보고하고 아무것도 정지하지 않는다.

## restore — 스냅샷 복원

```
- [ ] 최신 handoff 파일 읽기 (프로젝트명으로 탐색, 여러 개면 사용자에게 확인)
- [ ] 기준 SHA와 현재 HEAD 비교 — 다르면 그 사이 변경을 요약해 보고 (모른 척 진행 금지)
- [ ] 워킹트리 상태 대조 — 스냅샷의 더티 목록과 현재가 다르면 차이 보고
- [ ] 진행 장부 로드, "다음 단계" 항목부터 재개 제안
- [ ] 유예된 결정이 있으면 재개 전에 사용자에게 제시 (이때는 물어봐도 되는 시점이다)
```

## 이력

<details><summary>v1.1 (2026-08-08, 실전 검증 반영)</summary>

실전 1회 완료 (검증 세션 중간 스냅샷). save를 기본(이탈)과 --keep-lanes(중간 체크포인트)로 분리 —
기본 save의 무조건 레인 정지가 진행 중 CX 감사와 충돌한 사례에서. disallowed-tools 턴 전파 주의 추가.
</details>

<details><summary>v1 (2026-08-08)</summary>

family-plan-v2 기반 초판. GSD의 STATE.md 메타 차용하되 위치는 repo 밖(전역 문서 규약).
계정 회계 항목은 x20 2계정(개인/법인) 운용 지원. 경량 DoD 적용 대상 (베이스라인 실패 재현 생략,
실패 사례 서술로 대체: 컴팩션·계정 전환 시 진행 상태 유실).
</details>

---
skill: shared/
project: YNprinting
observed-version: 1.8.3
severity: minor
status: pending
---
## 실패 형태
문서 정합 결함 묶음 (사용 세션 CX 발견, 그 세션 T1 경로 실측):
① 더티 트리 허용(Preflight) ↔ 성공 기준 무조건 "클린 트리" — 기존 사용자 변경이 있으면
성공해도 못 만족 ② 기본 모드가 parallel-write인데 발진 체크리스트는 "5요소" 표기 — 소유권
요소 누락 가능 ③ sortie의 block 의미가 question-policy 공용 정의와 반대(갈래 보류는 gate)
④ shared/ 순환 참조 3쌍(codex-lane↔evidence-liveness 등) — skill-forge "1단계 참조" 규약 위반.
측정 불가 기준 4건: 성공 기준 "문서 정합"(검사 미정) / "full 차등·뮤테이션"(대상·시간 상한
없음) / "3회 시도" ↔ model-tiers "1회 후 승격"(회계 미정) / codex-lane major>5(중복·기각분
포함 여부 미정).
## 실측 증거
죽은 참조 0건·경로 전건 유효는 확인됨 — 결함은 링크가 아니라 의미 충돌.
## 재현/반증법
각 쌍의 원문 대조 (파일:조항 나열은 배치에서).
## 제안 (배치에서 판정)
일괄 정합 + validator에 순환·계약 토큰(5요소/6요소) 검사 검토.
배치 공통 금지선 (원 평가 D절 — 배치가 넘지 말 것): CX 최종 판정자 승격 금지 / 소유권·worktree
격리 완화 금지 / 측정 중 전면 정지 금지(현행 경합 한정이 옳음 — 18분 측정 중 문서 작업 실측) /
뮤테이션 목표를 전체 생존 0으로 변경 금지 / 자기 보고로 착지 판정 대체 금지 / 스킬 7개 통합
금지 / loop·sortie 슬래시 전용 제거 금지.
## v4.1 검증 실적 (배치 참고 — 건드리지 말 것)
두 번째 실전(레인 15·CX 6·웨이브 6)에서 v4.1 신설 조항 2건이 정확히 작동:
실측 배타 구간(관측 5) / 소유권 맵 실존 확인(배정 파일 전건 ls). 이번 케이스 7건은 전부
v4.1이 아직 안 겪은 축 — 배치 산출은 conductor v4.2로 명명하고, 근거를 "v4.1 이후 두 번째
실전 웨이브 관측 9건"으로 적는다.

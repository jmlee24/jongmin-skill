---
skill: jongmin-dev-conductor
observed-version: 1.9.2
project: YNprinting (2026-08-29~30 세션 — 착지 9커밋·쓰기 레인 5·리뷰 1·CX 6(+샌드박스 실패 2)·접합 major 5·minor 1·nit 1; 설치본 1.9.2 — conductor:207 pkill 경고 문구 인용으로 확인)
severity: major
status: pending
---
## 실패 형태
"리뷰 지적도 T1이 실측할 주장이다"(reviewer-prompt)를 T1이 어겼다 — 리뷰어의 ⑤축 지적을
검증 없이 실행 지시로 옮겼고 레인이 막았다. 규칙은 있는데 **순서를 강제하는 장치가 없다**:
접합 프롬프트를 쓰기 전에 실측을 통과했는지 확인하는 단계가 없다. v4.2(4883ca5)가 넣은 "도착
severity는 재현 전까지 provisional"·"리뷰 지적도 T1이 실측할 주장"(conductor 5절)은 **판정
원칙**이지 접합 프롬프트 작성 전 **순서 게이트**가 아니다 — 1.9.2에서 그대로 재발.
## 실측 증거
lane/guards: T1 지시 "⑤축 마커 테스트도 판정력 상실" → 레인 거부 근거 "그 테스트는
check_config_inventory를 부르고, _absence_owners는 check_py_citations 한 곳에서만 쓰이며, ④ 제거
커밋이 check_config_*를 한 줄도 안 건드렸다" — 전부 재현 확인. 시킨 대로 했으면 무관한 계약을
잠그는 오도된 테스트가 들어감.
## 재현/반증법
conductor 6절 리뷰 접합에 "지적 → 실행 지시 변환 전 T1 재현" 순서 조항 grep — 없음(5절에 원칙만).
## 제안 (배치에서 판정) — 보고서 우선순위 2
6절 리뷰 접합에 게이트 1줄: "지적을 실행 지시로 변환하기 전에 T1이 그 지적을 재현했는가.
재현 못 했으면 PLAUSIBLE로 표기해 레인에 넘기고, 레인의 반증을 정당한 산출물로 명시한다."
## 검증 실적 (배치 참고 — 건드리지 말 것)
- **사양 이탈 보고 요구가 최고 가치** — 레인이 T1 사양을 2회 거부, 2회 모두 옳았음(위 guards +
  lane/harness-fix: "메시지 확인만"으로 좁힌 지시에 대해 "커밋된 변경은 있고 워킹트리 clean이면
  훅이 침묵"을 근거로 폴백 체인 확장, 기각 대안 근거까지 기재). v1.7.1 "반박 5회 전부 실질
  가치"의 독립 추가 실측 2건.
- 착지 검증(자기 보고 불신 + 통합 트리 재실행) 작동 — 레인 보고 실패 1건이 환경 문제임을 직접
  재현(worktree TestCases/ 항목 1개 껍데기 — 8/20 worktree-env-manifest 케이스 3건째 관측),
  훅 major 결함을 T1 직접 재현.
- 회귀 가드 조건부 규칙 실효 — `if tables:`→`if False:` 뮤턴트가 착지 전 green·착지 후 red.

---
skill: shared/executor-prompt.md
project: YNprinting (2026-09-14 웨이브 — warplan→conductor→handoff, 13커밋·쓰기 6레인·리뷰 6+재확인)
observed-version: 1.12.1
severity: major
status: pending
related: 2026-08-30-1700-shared-landing-check-oracle-teeth-vs-defaults (오라클이 안 묻는 축 — 같은 계열), 2026-09-15-2127-jongmin-dev-conductor-recheck-template-cost (이 결함군의 재확인 비용)
---
## 실패 형태
상태 기계·게이트 태스크의 **위험 전이 반례**가 구현 후 리뷰에서야 나왔다 — 위상 ±1 지터로 조각 순회
재시작 / 조립 전 전이 / 재잠금 정지가 게이트를 영구히 닫음 / 게이트 시험이 항진명제. 리뷰 major 15건 →
fix 레인 2개(F·G) 58분 = **벽시계 28%**. 리뷰 층은 제 역할을 했지만(batch-guardrails 케이스 ①), 이
결함군은 사양 시점에 반례로 적을 수 있는 유형이었다 — 발견 위치가 구현 후로 밀린 것이 비용이다.
규약 쪽 구멍: executor-prompt 3) 검증 요구는 "가장 강한 오라클 / 등가성은 바이트 동일성 / 기준선
재잠금 방향"만 묻고(:17-21), **상태 전이 태스크에 "실패해야 할 반례"를 선기재하라는 항목이 없다**.
warplan 3절 `검증 오라클`(:48)도 같은 수준. oracle-teeth 케이스가 "기대값이 폴백과 구분되는가"를
안 묻는 축이었다면, 이번은 "어느 전이가 금지인가"를 안 묻는 축 — 둘 다 오라클 요구가 일반론에 머물러
태스크 유형별 이빨을 못 잡는 구조.
## 실측 증거
보고서 원문 문제 #2(근인: 편성 설계, 결과: major 15 → fix 레인 2개 58분/28%). 반례 4종은 리뷰어
뮤턴트 약 20종 전부 red로 오라클 강도 확인. 장부·SHA는 이 기기에 없음.
## 재현/반증법
- executor-prompt.md 3)·warplan 3절에 "반례"·"금지 전이" grep — 없음.
- 다음 상태 기계 태스크에서 프롬프트에 위험 전이 3~5개 + 반례를 선기재하고, 리뷰 major 중 "전이
  반례" 유형이 0에 가까워지는지 본다. 그래도 리뷰에서 같은 유형이 나오면 선기재로는 부족(리뷰 전
  CX-B 반박 축 지정이 대안).
## 제안 (배치에서 판정)
executor-prompt 3)에 조건부 1줄: **"상태 기계·게이트·잠금 로직이면 위험 전이 3~5개(지터·순서 역전·
재잠금·경계값)와 각각의 '실패해야 할 반례'를 프롬프트에 선기재한다 — 반례가 red가 아니면 항진명제다."**
반례 출처는 warplan CX 스카우트·CX-A(spec-key-table 케이스와 같은 명세 산출물에 절 하나 추가)로 —
T1이 직접 지어내지 않는다(t1-derived-numbers 축 회피).

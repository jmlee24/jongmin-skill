---
skill: shared/executor-prompt.md
project: YNprinting
observed-version: 1.8.3
severity: major
status: applied
applied-version: 1.9.0
---
## 실패 형태
"새 문서 파일 생성 금지"가 예시(:63)와 주의 절(:106)에만 있어 규약 복제(:23-25)의 필수 항목이
아니다 — 전역 규약에 있는 규칙인데 집행 지점이 없다. (조항 추가가 아니라 집행 수정)
## 실측 증거
문구를 넣지 않던 저장소: 신규 `.md` 30일 59건(≈2/일). 넣은 웨이브: 0건.
## 재현/반증법
executor 프롬프트 실물에서 규약 복제 절에 해당 문구 유무 grep → 없는 웨이브의 `git log --diff-filter=A -- '*.md'` 집계.
## 제안 (배치에서 판정)
:23-25 `4) 규약 복제` 플레이스홀더에 기본 포함 항목 명시:

4) 규약 복제
   <프로젝트 메모리·수치 표기 규약 등, 에이전트가 볼 수 없는 규칙의 전문을 여기 붙인다.
    에이전트는 이 프롬프트 밖의 컨텍스트를 못 본다.>

   🔴 **아래는 프로젝트와 무관하게 항상 복제한다** (없으면 발진 금지):
   - **새 `.md` 파일 생성 금지** — 계획·요약·보고는 응답 본문으로. 임무가 명시 요구할 때만 예외
   - 사양 이탈은 **멈추고 실측 근거와 함께 보고** (5요소 참조)

삽입 공간은 실물 예시(:37-69) 축소분(별도 케이스 1-c)으로 확보.

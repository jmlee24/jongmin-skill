---
skill: shared/landing-check.md
project: YNprinting
observed-version: 1.8.3
severity: major
status: pending
---
## 실패 형태
① 새 가드·정적 검사·CI 규칙이 양성 오라클 없이 착지 — 가드를 무매치로 바꿔도 green.
② impact-scope 4범주(:28-33)가 정의돼 있는데 기입란이 없어 읽지 않고 착지마다 전량 실행.
## 실측 증거
① 정규식 가드가 ③(표기 변형) 없이 착지 → 리뷰가 "양성 오라클 0건" 판정(무매치 뮤턴트 생존).
   이후 조사·콜론 표기 변형이 새는 것이 발견돼 닫힘.
② 5회 × 3분 = 15분 낭비 (2026-08-21 평가 세션).
## 재현/반증법
① 가드 패턴을 절대 매치 안 되는 값으로 바꿔 테스트 실행 — green이면 재현.
② 시간 장부에서 impact-scope 선택 사유 기재 0건 확인.
## 제안 (배치에서 판정)
2-a. `## 자동 full 승격`(:49) 다음, `## 공통`(:54) 앞에 새 절:

## 가드 수용 게이트 — 새 가드·검사기·CI 규칙에만

새 가드·정적 검사·CI 판정 규칙은 **착지 전에 최소 3가지를 증명**한다:

① **대표 정상 입력이 green** — 현행 저장소 상태가 통과하는지
② **그 가드가 금지한다고 주장하는 정확한 위반 1건이 red**
③ **같은 위반의 현실적 표기 변형도 red** — 지원하지 않는 변형이 있으면
   그 경계를 **규칙 설명과 테스트명에 명시**한다(못 잡는 것을 적는다)

②·③은 가능하면 **뮤턴트 또는 최소 위반 픽스처**로 실행한다.
가드를 무매치로 바꿔도 green이면 그 가드는 오라클이 없는 것이다.

⚠️ **일반 단위 시험에는 적용하지 않는다** — 가드·검사기·CI 규칙 한정.

3-b. :28-33 아래 기입란:
- [ ] **발진 전 4칸을 채운다** — 각 칸에 실행할 명령 또는 `해당 없음 + 근거`:
      직접 테스트 / 1-hop 호출자 / touched package / fixture·schema·config
      "안전하니 full"도 허용하되 **그 사유와 추가 소요시간을 시간 장부에 남긴다**
